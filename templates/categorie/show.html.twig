{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-secondary" href="{{ path('app_categorie_index') }}">Retour à la liste</a>

    <a class="btn btn-outline-primary" href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">Modifier</a>

    <a{{ include('categorie/_delete_form.html.twig') }}</a>
    <br><br>
{% endblock %}
