<form method="get" action="{{ path('app_tricks_delete', {'id': trick.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette image ?');">
    <input type="hidden" name="_token" value="{{ ('delete' ~ trick.id) }}">
    <button class="btn btn-danger" type="submit">Supprimer</button>
</form>
